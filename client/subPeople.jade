template(name="subPeople")
  h3 People
  table.ui.very.basic.celled.table
    thead
      tr
        th Person
        th Min (#{totalMin})
        th Percent
    tbody
      each shares
        tr
          td
            h4.ui.image.header
              img.ui.mini.rounded.image(src='/logo.png')
              .content #{username}
                .sub.header
                  | Placeholder
          td #{min}
          td #{percent}%

