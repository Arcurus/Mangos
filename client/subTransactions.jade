template(name="subTransactions")
  .ui.feed
    each transactions
      .event
        .label
          img(src='/logo.png')
        .content
          .date #{createdAt}
          .summary
            p Received #{amount} from #{from}
